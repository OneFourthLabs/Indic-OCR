## Indian Documents Information Extraction

### Pre-requisites

1. Clone the Indic-OCR repo first, and then set its path in your terminal to the environment variable `INDIC_OCR_HOME`

### Running the UI

- `streamlit run ui.py`
